---
const currentPath = Astro.url.pathname;

const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/newstadium', label: 'New Stadium' },
  { href: '/newdemos', label: 'New Demos' },
  { href: '/newbuilds', label: 'New Builds' },
  { href: '/theotherstuff', label: 'The Other Stuff Podcast' },
];

function isActive(href: string, current: string): boolean {
  if (href === '/') {
    return current === '/' || current === '';
  }
  return current.startsWith(href);
}
---

<header>
  <nav>
    <div class="nav-links">
      {navLinks.map(link => (
        <a
          href={link.href}
          class:list={[{ active: isActive(link.href, currentPath) }]}
        >
          {link.label}
        </a>
      ))}
    </div>
  </nav>
</header>

<style>
  header {
    width: 100%;
    margin: 0;
    padding: 1.5rem 1.5rem 1rem;
    font-size: var(--font-size-base);
  }

  nav {
    display: flex;
    justify-content: flex-start;
    align-items: center;
    width: 100%;
  }

  .nav-links {
    display: flex;
    gap: clamp(0.25rem, 2vw, var(--spacing-lg));
    align-items: center;
    flex-wrap: nowrap;
  }

  .nav-links a {
    color: var(--text-muted);
    text-decoration: none;
  }

  .nav-links a::after {
    content: none;
  }

  .nav-links a:hover {
    color: var(--link-hover);
  }

  .nav-links a.active {
    color: var(--text);
  }

  /* Mobile */
  @media (max-width: 600px) {
    header {
      padding: 1rem 1rem 0.75rem;
    }

    .nav-links {
      flex-wrap: wrap;
      gap: var(--spacing-md);
    }
  }
</style>
